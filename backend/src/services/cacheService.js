const getCache = async (_key) => null;
const setCache = async (_key, _value, _ttlSeconds) => null;
const invalidateCache = async (_key) => null;

module.exports = {
  getCache,
  setCache,
  invalidateCache,
};
